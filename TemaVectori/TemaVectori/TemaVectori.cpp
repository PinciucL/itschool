#include <iostream>
#include "string.h"
#include <math.h>
#include <vector>
#include <set>
#include <algorithm>

//Creati un std::vector<int> cu valorile 2, 4, 6,8 si 10. Calculati si afisati suma tuturor elementelor din vector folosindg un iterator pentru a parcurge vectorul

//int main() 
//{
//    std::vector<int> numbers = { 2, 4, 6, 8, 10 };
//    int sum = 0;
//
//    for (std::vector<int>::iterator it = numbers.begin(); it != numbers.end(); ++it) 
//    {
//        sum += *it;
//    }
//
//    std::cout << "Suma elementelor din vector este: " << sum << std::endl;
//
//    return 0;
//}

//Creati un std::vector<int> cu valorile 1,2,3,4 si 5. Inversati ordinea elementelor din vector si apoi afisati toate elementele vectorului inversat.

//int main() 
//{
//    std::vector<int> numbers = { 1, 2, 3, 4, 5 };
//
//    std::reverse(numbers.begin(), numbers.end());
//
//    std::cout << "Numerele inversate sunt : ";
//    
//    for (int num : numbers) 
//    {
//        std::cout << num << " ";
//    }
//    std::cout << std::endl;
//
//    return 0;
//}

//Creati un std::vector<int> cu valorile 1,2,2,3,4,4 si 5. Eliminati elementele duplicate astfel incat vectorul sa contina doar elemente unice si apoi afisati toate elementele vectorului dupa eliminarea duplicatelor.

//int main() 
//{
//    std::vector<int> numbers = { 1, 2, 2, 3, 4, 4, 5 };
//
//    std::sort(numbers.begin(), numbers.end());
//
//    auto last = std::unique(numbers.begin(), numbers.end());
//
//    numbers.erase(last, numbers.end());
//
//    std::cout << "Numarul fara duplicate este : ";
//    
//    for (int num : numbers) 
//    {
//        std::cout << num << " ";
//    }
//    std::cout << std::endl;
//
//    return 0;
//}


